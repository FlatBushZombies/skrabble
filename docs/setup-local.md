# Local Setup


This guide helps you start the entire stack locally for development.


## Requirements
- Docker & Docker Compose
- Python 3.11+
- Node.js (optional, for frontends)


## Start the stack


```bash
# build and run
docker compose up --build